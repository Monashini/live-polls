# Base URL of the Go backend. No trailing slash.
#
# Vite only exposes variables prefixed with VITE_ to the browser, and anything
# here IS public -- it ships inside the JavaScript bundle. Never put a secret
# in this file.
#
# Local development:
VITE_API_BASE_URL=http://localhost:8080
